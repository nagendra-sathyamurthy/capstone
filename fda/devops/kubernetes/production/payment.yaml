# Production - Payment Service with Dedicated MongoDB
# Dedicated MongoDB instance for payment service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-mongodb-deployment
  namespace: capstone-services
  labels:
    app: payment-mongodb
    environment: production
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment-mongodb
  template:
    metadata:
      labels:
        app: payment-mongodb
        environment: production
    spec:
      containers:
      - name: mongodb
        image: mongo:latest
        ports:
        - containerPort: 27017
        env:
        - name: MONGO_INITDB_ROOT_USERNAME
          valueFrom:
            secretKeyRef:
              name: payment-mongodb-secret
              key: username
        - name: MONGO_INITDB_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: payment-mongodb-secret
              key: password
        - name: MONGO_INITDB_DATABASE
          value: "paymentdb"
        resources:
          requests:
            memory: "512Mi"
            cpu: "200m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        volumeMounts:
        - name: payment-mongodb-storage
          mountPath: /data/db
      volumes:
      - name: payment-mongodb-storage
        persistentVolumeClaim:
          claimName: payment-mongodb-pvc
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: payment-mongodb-pvc
  namespace: capstone-services
  labels:
    environment: production
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
---
apiVersion: v1
kind: Service
metadata:
  name: payment-mongodb-service
  namespace: capstone-services
  labels:
    environment: production
spec:
  selector:
    app: payment-mongodb
  ports:
  - protocol: TCP
    port: 27017
    targetPort: 27017
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-deployment
  namespace: capstone-services
  labels:
    app: payment
    environment: production
spec:
  replicas: 2
  selector:
    matchLabels:
      app: payment
  template:
    metadata:
      labels:
        app: payment
        environment: production
    spec:
      containers:
      - name: payment
        image: services-payment:latest
        ports:
        - containerPort: 8080
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: "Production"
        - name: ConnectionStrings__DefaultConnection
          valueFrom:
            secretKeyRef:
              name: payment-mongodb-secret
              key: connection-string
        - name: DatabaseSettings__ConnectionString
          valueFrom:
            secretKeyRef:
              name: payment-mongodb-secret
              key: connection-string
        - name: DatabaseSettings__DatabaseName
          value: "paymentdb"
        - name: DatabaseSettings__CollectionName
          value: "payments"
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: capstone-services
  labels:
    environment: production
spec:
  selector:
    app: payment
  ports:
  - protocol: TCP
    port: 30006
    targetPort: 8080
  type: LoadBalancer
