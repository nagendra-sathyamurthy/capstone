# Local Development MongoDB Configuration
apiVersion: v1
kind: ConfigMap
metadata:
  name: mongodb-config
  namespace: capstone-services
  labels:
    environment: local
data:
  database-name: "admin"  # Default initialization database