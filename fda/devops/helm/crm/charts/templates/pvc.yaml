apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "crm-mongodb.fullname" . }}-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.persistence.size }}
