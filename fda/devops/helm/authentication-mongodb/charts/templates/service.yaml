apiVersion: v1
kind: Service
metadata:
  name: {{ include "authentication-mongodb.fullname" . }}
  labels:
    {{- include "authentication-mongodb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: mongodb
  selector:
    {{- include "authentication-mongodb.selectorLabels" . | nindent 4 }}
